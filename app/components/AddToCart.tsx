'use client';

import React from 'react'

export const AddToCart = () => {
    return (
        <div>
            <button className="btn btn-outline btn-accent" onClick={() => console.log('Click')}> Add to Cart</button>
        </div>
    )
}
